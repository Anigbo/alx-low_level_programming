#include "main.h"
#include <stdio.h>

/**
 * main - prints its name
 * @agrc: argument count
 * @argv: argument vector
 *
 * Return: Always zero
 */
int main(int agrc, char *agrv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
