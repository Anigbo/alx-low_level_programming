#include "main.h"

/**
 * mul - multiplies 2 numbers
 * @a: first number to be multipled
 * @b: second number to be multipled with first
 *
 * Return: The result of multiplication a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
